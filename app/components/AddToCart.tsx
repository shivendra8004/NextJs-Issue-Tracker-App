"use client";
import React from "react";

const AddToCart = () => {
    return (
        <div>
            <button onClick={() => console.log("Clicked")}>Click</button>
        </div>
    );
};

export default AddToCart;
